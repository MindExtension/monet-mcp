# Per-server config (not credentials — credentials come from MCP client headers)

# Monet API base URL — only override if you run a non-default Monet instance
MONET_BASE_URL=https://e.monet.lt/DesktopModules/MonetServices2/API/V2

# Optional: log level (info | debug)
LOG_LEVEL=info
